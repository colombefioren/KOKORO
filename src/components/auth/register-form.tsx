"use client"

import { useForm } from "react-hook-form";
import { Button } from "../ui/button";

const RegisterForm = ({
  className,
  onToggle,
}: {
  className?: string;
  onToggle: () => void;
}) => {
  const form = useForm():
};
export default RegisterForm;
